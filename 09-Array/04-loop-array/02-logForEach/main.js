//use for Each
const names = ['Mike', 'Sid', 'Jack', 'Bill'];
// names.forEach((item,index,array)=>{console.log(`${index+1}. ${item}`)})

names.forEach((item, index, array) => console.log(`${index + 1}. ${item}`));
